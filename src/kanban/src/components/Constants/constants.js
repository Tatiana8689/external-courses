const main = document.getElementById('tasks-board');
const createNewBoard = document.getElementById('new-list-btn')
const activeCount = document.getElementById('active-count');
const finishedCount = document.getElementById('finished-count');

export { activeCount, finishedCount, main, createNewBoard }
