let typeFunction = (a) => {
   if (typeof a === 'number') {
		if (isNaN(a)) {
		    return underfined
		}
        return typeof a;
  	} 
   if (typeof a === 'string') {
        return typeof a;
    } 
    return 'underfined';
}
module.exports = typeFunction;